bazel-bin
bazel-out
bazel-stablehlo
bazel-testlogs
build
cmake-build
llvm-project
llvm-build
.vscode
.cache
.idea/
**/__pycache__
*.whl
**/*.egg-info/
/Testing/
.venv

# Root-level session binaries/snapshot (#97 final-review wave, M13).
# Leading slash anchors each pattern to the repo root only, so it can
# never match the tracked calibration/ DIRECTORY (which has no .json
# suffix) or any nested bench/rtrack/ path.
/bench-rtrack
/bench-cpu-rooflines
/calibration.json
